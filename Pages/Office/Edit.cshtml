@page
@model ge_repository.Pages.Office.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>ge_office</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="office.Id" />
            <input type="hidden" asp-for="office.createdDT"/>
            <input type="hidden" asp-for="office.createdId"/>
            <input type="hidden" asp-for="office.editedDT"/>
            <input type="hidden" asp-for="office.editedId"/> 
           <partial name="_inputOffice"
                    for="office"
                    view-data="ViewData" />
           <partial name="_inputLocation"
                    for="office"
                    view-data="ViewData" />

            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

