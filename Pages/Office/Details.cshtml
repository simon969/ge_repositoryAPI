@page
@model ge_repository.Pages.Office.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>ge_office</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.office.name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.office.name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.office.manager)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.office.manager.Id)
        </dd>
        <partial name="_DetailsLocation"
                    for="office"
                    view-data="ViewData" />
        <partial name="_DetailsBase"
                    for="office"
                    view-data="ViewData" />
       
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.office.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
